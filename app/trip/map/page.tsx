export default function MapPage() {
  return (
    <main className="mx-auto max-w-3xl p-6">
      <p className="text-sm text-zinc-500">
        Map view arrives in the next build phase — use Agenda to add stops
        for now.
      </p>
    </main>
  );
}
